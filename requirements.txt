# Core dependencies
python-dotenv==1.0.0
PyYAML==6.0.1

# YouTube
yt-dlp
google-api-python-client==2.108.0
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Audio processing
pydub==0.25.1
demucs  # Separación de audio (más moderno que Spleeter)
openai-whisper
torch  # Para Whisper (CPU o GPU) - versión automática según tu sistema
torchaudio

# Video generation
ffmpeg-python==0.2.0

# Database
sqlalchemy==2.0.23

# Utilities
requests==2.31.0
tqdm==4.66.1
colorama==0.4.6

# Logging
loguru==0.7.2

# Optional: Replicate API (si prefieres usar APIs en vez de local)
# replicate==0.22.0

# Development
black
pytest
